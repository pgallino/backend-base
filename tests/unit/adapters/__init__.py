"""Unit tests for adapters (placeholder).

This package exists to group adapter unit tests (HTTP adapters, CLI adapters,
etc.). Add tests here as they are created. Keeping the package explicit makes
`pytest tests/unit` discovery clearer.
"""
